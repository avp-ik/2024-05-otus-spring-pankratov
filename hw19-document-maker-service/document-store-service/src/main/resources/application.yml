endpoint: "https://play.min.io"
bucketName: "2024-05-otus-spring-pankratov"
accessKey: "Q3AM3UQ867SPQQA43P2F"
secretKey: "zuf+tfteSlswRu7BJ86wekitnifILbZam1KYY3TG"

server:
  port: 8080
  max-http-request-header-size: 2048KB

spring:
  output:
    ansi:
      enabled: always

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  data:
    rest:
      basePath: /datarest

management:
  endpoints:
    web:
      exposure:
        include: "metrics,health,logfile,prometheus"
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true
  jmx:
    enabled: true

logging:
  level:
    ROOT: DEBUG
  file:
    name: ./logs/hw16-actuator.log